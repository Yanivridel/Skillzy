export interface IFormDataSingUp {
    fName: string;
    lName: string;
    phone: string;
    email: string;
    password: string;
    role: string;
}
export interface IFormDataLogIn {
    email: string;
    password: string;
}